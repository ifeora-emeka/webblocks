export interface PagesData {
  _id: string
  name: string
  route: string;
  createdAt: string
  updateAt: string
}

export interface PageMetadata {
  _id: string
  name: string
  description: string
}
